<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MobileQuiz.Views.ManagerQuestions.ManagerQuestionsView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:material="clr-namespace:XF.Material.Forms.UI;assembly=XF.Material"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Gerenciar Questoes"
    Visual="Material"
    mc:Ignorable="d">
    <CollectionView HorizontalOptions="FillAndExpand" ItemsSource="{Binding QuestionOptions}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout Padding="10" Orientation="Vertical">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ActionOpen}" />
                    </StackLayout.GestureRecognizers>
                    <material:MaterialCard CornerRadius="10" Elevation="2">
                        <StackLayout Margin="0,0" Orientation="Horizontal">
                            <Image
                                HeightRequest="60"
                                Source="{Binding ActionImage}"
                                VerticalOptions="Center"
                                WidthRequest="60" />

                            <StackLayout
                                Margin="20,0,0,0"
                                Orientation="Vertical"
                                VerticalOptions="Center">
                                <Label
                                    Grid.Row="0"
                                    FontAttributes="Bold"
                                    FontSize="Medium"
                                    Text="{Binding ActionName}"
                                    VerticalOptions="Center" />
                                <Label
                                    Grid.Row="1"
                                    FontSize="Subtitle"
                                    Opacity=".6"
                                    Text="{Binding ActionDescription}"
                                    TextColor="Default" />
                            </StackLayout>
                        </StackLayout>
                    </material:MaterialCard>
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>